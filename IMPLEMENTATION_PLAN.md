# anchor-audit — Implementation Plan

Derived from [PRD.md](./PRD.md). Six phases, each with a checkpoint that must pass
before the next phase starts.

---

## Phase 1 — Scaffolding

**Deliverables**
- Full repo directory layout per PRD §2 (placeholder dirs kept with `.gitkeep`)
- Root `package.json` (private npm-workspaces root) and `tsconfig.json` (strict, Node 20 / NodeNext)
- `cli/package.json` for the publishable `anchor-audit` package with `bin` entry and stub sources
  (`index.ts`, `scanner.ts`, `auditor.ts`, `reporter.ts`, `rules-loader.ts`)
- `README.md` skeleton with all 8 sections from PRD §7
- `SKILL.md` skeleton with final frontmatter and placeholder body sections (PRD §3)
- `LICENSE` (MIT)
- `.github/workflows/ci.yml` running typecheck + lint + test
- ESLint config, vitest wiring, one smoke test so CI is green from day one

**Checkpoint 1 — ✅ passed 2026-06-10**
- [x] `npm install` succeeds at repo root
- [x] `npm run typecheck`, `npm run lint`, `npm test` all pass locally
- [x] `node cli/dist/index.js --help` prints the CLI help with all PRD §5 flags
- [x] Repo layout matches PRD §2

---

## Phase 2 — Rule catalog (50 rules)

**Deliverables**
- All 50 rule files in `/rules/` following the exact template in PRD §4.2
- Every rule cites sources from PRD §4.3 (Neodyme, Sec3, Helius, Anchor book, Cyfrin, public audits) — no invented content
- `rules/INDEX.md` table: ID, name, severity, category
- `rules/README.md` explaining the template and contribution rules
- `SKILL.md` body filled in: workflow, rule catalog links, output format, limitations

**Process:** batches of ~10 rules (matching the PRD categories), review each batch before the next.

**Checkpoint 2**
- [ ] 50 files exist, names match PRD §4.1 exactly
- [ ] Each file has all 7 template sections and ≥2 references
- [ ] `INDEX.md` row count = 50, severities assigned
- [ ] `SKILL.md` < 600 lines and links to every rule

---

## Phase 3 — Vulnerable example program

**Deliverables**
- `examples/01-vulnerable-vault/`: intentionally buggy Anchor vault (~300–400 lines) under `programs/`
- Triggers ≥15 distinct rules across ≥5 categories
- `EXPECTED_FINDINGS.md`: rule ID → file:line → short description (this is the CLI regression oracle)
- `examples/01-vulnerable-vault/README.md` with warnings (never deploy)

**Checkpoint 3**
- [ ] Program compiles conceptually as valid Anchor syntax (cargo check if toolchain available; otherwise manual review)
- [ ] ≥15 rules represented and documented in `EXPECTED_FINDINGS.md`

---

## Phase 4 — CLI implementation

**Deliverables**
- `scanner.ts`: collect/filter `.rs` files under target path, validate Anchor layout
- `rules-loader.ts`: load `/rules/*.md` from the installed package, parse ID/severity/category
- `auditor.ts`: Claude API orchestration — rules batched 4–6 per call, structured JSON findings, model `claude-sonnet-4-6` default
- `reporter.ts`: markdown (PRD §5 format) + JSON output, severity sorting, exit code 1 on critical/high
- All flags: `--output`, `--rules`, `--severity`, `--format`, `--verbose`, `--api-key`, `--model`
- Unit tests against `tests/fixtures/` snippets; documented JSON schema for findings

**Checkpoint 4**
- [ ] `npm install -g .` works; `anchor-audit examples/01-vulnerable-vault` produces a report
- [ ] Findings match `EXPECTED_FINDINGS.md` (iterate on prompts until they do)
- [ ] JSON output validates against the documented schema
- [ ] CI green

---

## Phase 5 — Real-world audits

**Deliverables**
- Pick two OSS, permissively-licensed, non-trivial Anchor programs
- Run the actual CLI against each; save outputs as `examples/02-real-program-audit/AUDIT_REPORT.md` and `examples/03-real-program-audit/AUDIT_REPORT.md`
- No PRs filed against the upstream repos

**Checkpoint 5**
- [ ] Both reports generated by the tool itself (not hand-written), with tool version + date stamps

---

## Phase 6 — Polish and release

**Deliverables**
- README final pass: install (skill + CLI), usage, full 50-row rule table, limitations/disclaimer, contributing
- `ROADMAP.md` with PRD §9 stretch goals
- Demo recording, final cleanup, public release

**Checkpoint 6 (= PRD §8 acceptance criteria)**
- [ ] All PRD acceptance boxes checked
- [ ] Clean commit history, MIT license, repo public
