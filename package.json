{
  "name": "anchor-audit-repo",
  "private": true,
  "type": "module",
  "description": "Workspace root for anchor-audit: a security audit toolkit for Anchor programs on Solana",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "cli"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "build": "npm run build --workspace anchor-audit"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.19.0",
    "vitest": "^2.1.8"
  }
}
